[project]
name = "sync-demo-backend"
version = "0.1.0"
description = "Fake third-party backend for sync demo"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.8",
    "httpx==0.28.1",
    "uvicorn[standard]==0.34.0",
]
